{
  "name": "penumbra-annotator",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "serve": "node build.mjs && npx --yes serve -l 8080 ."
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.2"
  }
}
